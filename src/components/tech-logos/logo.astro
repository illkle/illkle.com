---
import { getIcon, type TechName } from "./logos";
const { icon, size } = Astro.props as { icon: TechName; size: string };
const i = getIcon(icon);

const s = size ?? "12";
---

<a href={i.link} target="_blank">
  <img alt={i.alt} src={i.image} style={`height: ${s}px`} />
</a>
